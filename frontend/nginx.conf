server {
    listen 80;
    root /usr/share/nginx/html;

    # /api/* is routed directly to the server service by Traefik
    # (see traefik-static.yml.j2) and never reaches this container.
    location / {
        try_files $uri /index.html;
    }
}
