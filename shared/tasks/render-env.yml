---
# Single-mode .env (browser-facing frontends); the caller sets `mode`.
- include_tasks: "{{ formation_root }}/shared/tasks/merge-env.yml"

- include_tasks: "{{ formation_root }}/shared/tasks/write-env.yml"
