---
# Forwarding shim: frontend/formation.yml includes tasks via
# "{{ playbook_dir }}/tasks/render-env.yml" (playbook_dir = this project's
# directory), so the real, shared logic lives one level up.
- include_tasks: "{{ formation_root }}/shared/tasks/render-env.yml"
