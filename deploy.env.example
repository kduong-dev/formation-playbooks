# Copy to deploy.env (gitignored) and fill in. Read by every deploy script;
# SERVER / REMOTE_ROOT in the environment override these.

# Anything ssh accepts: an alias from ~/.ssh/config, or user@host.
SERVER=my-server
REMOTE_ROOT=/opt/formation
